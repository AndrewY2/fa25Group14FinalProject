@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService AuthorizationService

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Bevo's Books</title>
    <link href="~/lib/twitter-bootstrap/css/bootstrap.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
</head>
<body>
    <div>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">

            <a class="navbar-brand px-3" asp-controller="Home" asp-action="Index">Bevo's Books</a>

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-content"
                    aria-controls="nav-content" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="nav-content">
                <ul class="navbar-nav mr-auto">

                    <li class="nav-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link"><i class="fas fa-home"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="Books" asp-action="Search" class="nav-link"><i class="fas fa-book-open"></i> Books</a>
                    </li>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a asp-controller="Orders" asp-action="Index" class="nav-link"><i class="fas fa-receipt"></i> Orders</a>
                        </li>

                        @if (User.IsInRole("Admin") || User.IsInRole("Employee"))
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="managementDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-users-cog"></i> Management
                                </a>
                                <div class="dropdown-menu" aria-labelledby="managementDropdown">
                                    <h6 class="dropdown-header">Customer Relations</h6>
                                    <a class="dropdown-item" asp-controller="Employees" asp-action="ManageCustomers">Manage Customers</a>

                                    <div class="dropdown-divider"></div>

                                    <h6 class="dropdown-header">Reviews & Approvals</h6>
                                    <a class="dropdown-item" asp-controller="Reviews" asp-action="Pending">Review Queue</a>

                                    @if (User.IsInRole("Admin"))
                                    {
                                        <div class="dropdown-divider"></div>
                                        <h6 class="dropdown-header">Employee Administration</h6>
                                        <a class="dropdown-item" asp-controller="Employees" asp-action="ManageEmployees">Manage Employees</a>
                                    }
                                </div>
                            </li>
                        }

                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="operationsDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-warehouse"></i> Admin Tools
                                </a>
                                <div class="dropdown-menu" aria-labelledby="operationsDropdown">
                                    <h6 class="dropdown-header">Procurement & Sales</h6>
                                    <a class="dropdown-item" asp-controller="Promotions" asp-action="Index">Promotions & Coupons</a>
                                    <a class="dropdown-item" asp-controller="Reorders" asp-action="Index">Book Procurement</a>

                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" asp-controller="Reports" asp-action="Index">Reports Dashboard</a>
                                </div>
                            </li>
                        }

<<<<<<< Updated upstream
                        @* REMOVED: Redundant "Manage Account" link *@
=======

                        @* Customer-only account page *@
                        @if (!User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Account" asp-action="Index" class="nav-link">Manage Account</a>
                            </li>
                        }

                        @* Employee/Admin review approval queue *@
                        @if (User.IsInRole("Admin") || User.IsInRole("Employee"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Reviews" asp-action="Pending" class="nav-link">Review Queue</a>
                            </li>
                        }


                        @* Admin-only tools *@
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a asp-controller="RoleAdmin" asp-action="Index" class="nav-link">Manage Users</a>
                            </li>
                            <li class="nav-item">
                                <a asp-controller="Promotions" asp-action="Index" class="nav-link">Promotions</a>
                            </li>
                            <li class="nav-item">
                                <a asp-controller="Reorders" asp-action="Index" class="nav-link">Book Procurement</a>
                            </li>
                            <li class="nav-item">
                                <a asp-controller="Reports" asp-action="Index" class="nav-link">Reports</a>
                            </li>
                        }
>>>>>>> Stashed changes
                    }
                </ul>
            </div>

            <div class="navbar-nav d-flex align-items-center">

                @* CART: Placed outside main UL and right-aligned *@
                @if (User.IsInRole("Customer"))
                {
                    <a asp-controller="OrderDetails" asp-action="Index" class="nav-link text-white btn btn-sm btn-outline-light mr-2">
                        <i class="fas fa-shopping-cart"></i> Cart
                    </a>
                }

                @await Html.PartialAsync("_LoginPartial")
            </div>
        </nav>
    </div>

    <div class="container">
        @RenderBody()
    </div>

    <script src="~/lib/jquery/jquery.min.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js"
            integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
            crossorigin="anonymous"></script>

    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
            integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
            crossorigin="anonymous"></script>

    <script src="~/lib/jquery-validate/jquery.validate.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js"></script>

</body>
</html>